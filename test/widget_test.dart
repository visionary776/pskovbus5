// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

//import 'package:flutter_test/flutter_test.dart';

import 'package:pskov_bus_5/repository/repository.dart';
import 'package:pskov_bus_5/models/result.dart';
import 'package:test/test.dart';

void main() {




  test ("fetchdata", (){
 //   var repository=Repository();
   // var result=Result("",[],[]);
//Result result;
    for (int i=10;i<30;i++){
     // result= repository.fetchData(i);



      //  print("Test result: ${result.routes.toString()}");
    //  print("_____routes:  ${result.routes.toString()}");
      //  print("Test result _______timeTable:  ${result.timeTable.toString()} ");

    }

  });





}
